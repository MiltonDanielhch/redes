# Ubicación: `crates/application/Cargo.toml`
#
# Descripción: Capa de aplicación - casos de uso.
#               Depende solo de domain.
#
# ADRs relacionados: 0001 (Hexagonal)

[package]
name = "application"
version = "0.1.0"
edition = "2024"

[dependencies]
domain = { path = "../domain" }
uuid = { workspace = true }